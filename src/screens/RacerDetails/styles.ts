import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
    fontSize: 16,
    margin: 5,
    paddingTop: 30,
  },
});

export default styles;
